services:
  - type: web
    name: journey-rentals-api
    runtime: node
    plan: starter
    rootDir: api
    nodeVersion: "20"
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
